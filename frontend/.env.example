# Vite development server
VITE_DEV_PORT=3000

# Local /api, /v1 and /setup proxy target
VITE_DEV_PROXY_TARGET=http://localhost:8080

# Keep browser requests same-origin; Pages Functions proxies them to API_UPSTREAM.
VITE_API_BASE_URL=/api/v1
